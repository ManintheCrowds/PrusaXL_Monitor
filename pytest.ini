# PURPOSE: Pytest configuration for PrusaXL_Monitor.
[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: Unit tests (fast, no external deps).
    e2e: End-to-end tests (may use DB).
    smoke: Smoke tests (quick sanity check).
